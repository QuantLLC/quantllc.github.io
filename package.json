{
  "name": "quant-downloads",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Quant download website with email/password authentication and email verification",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "emulators": "firebase emulators:start --project demo-quant-downloads",
    "dev:all": "concurrently -n emulators,vite -c blue,green \"npm run emulators\" \"npm run dev\""
  },
  "dependencies": {
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "firebase-tools": "^13.0.0",
    "vite": "^6.0.0"
  }
}
